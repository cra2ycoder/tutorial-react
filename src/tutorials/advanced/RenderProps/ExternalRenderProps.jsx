import React from 'react'
import { renderImage, renderTitle, renderParagraph } from './renderProps'

class ExternalRenderProps extends React.Component {
  render() {
    return ''
  }
}

export { ExternalRenderProps }
export default ExternalRenderProps
